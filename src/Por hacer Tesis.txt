- Mejorar al regresar de page-user la tabla de usuarios vuelve a la primera pagina
(solucion crear decoradores @Input y @Output)

*** Problemas perfiles de usuario (Pueden ver los demas perfiles) siendo usuarios --Interceptor o Guard puede ayduar


*** Al momento de convertir de JPG to dicom
    -enviar el jpg directo al servidor de prediccion(Modificar flask para esto - solo acepta .dcm)
    -el .dcm convertidor pasarle directo a la variable File() del meotodo  "stackDicom()"
    -se renderiza primero el dicom y luego la prediccion




////////-------- CODIGO PARA VER TODO DATA DEL CANVAS-------------/////////////////////
const enabledElements = cornerstone.getEnabledElements();

    const element2 = enabledElements.find((ee) => {
      console.log("datos del canvas: ", ee);
      ee.image.imageId === 'dicomfile:0'
      console.log("Numero de Archivo actual: ", ee.image.imageId);

    })
////////^^^^^^^ CODIGO PARA VER TODO DATA DEL CANVAS ^^^^^^^ /////////////////////
